openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts App
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description:
    <опис вашого додатку>
tags:
  - name: Contacts
    description: Operations about contacts.  
  # теги, що ви будете використовувати
servers:
  - url: http://localhost:3000
  - url: https://nodejs-hw-mongodb-swagger-egc4.onrender.com #посилання на задеплоєний сайт
paths:
  /students:
    get:
      $ref: ../swagger/paths/contacts/get.yam

  /contacts/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml

  /contacts:
    post:
      $ref: ../swagger/paths/contacts/post.yaml
  # тут будуть посилання на ендпоінти
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer